/** Injected by vite `define` at build time (see vite.config.ts) — the hub footer's build stamp.
 *  Dev builds get the dev-server start time, which is exactly what you want to see there. */
declare const __BUILD_STAMP__: string
